import styled from "styled-components";

export const Container = styled.header`
  position: fixed;
  width: 100%;
  left: 0px;
  top: 0px;
  /* overflow-x: hidden; */
  background: var(--body-color);
`;
